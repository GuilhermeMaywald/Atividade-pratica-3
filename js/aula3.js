{ // Resposta 
    let manipulandoStrings = 'Manipulando strings com JS';
    alert(manipulandoStrings.toUpperCase());

    console.log(manipulandoStrings.slice(0,5));

    alert(manipulandoStrings.length);

    let concatenandoStrings = 'Vamos concatenar as duas strings';

    console.log(manipulandoStrings + ' ' + concatenandoStrings);
}